# Security

This app reads local Codex session metadata and JSONL token counters only. It
does not send usage data over the network; the dashboard menu item only opens
the ChatGPT usage dashboard in the user's default browser.

Report security issues privately through the GitHub repository security
advisory flow.
